{
  "name": "@teimurjan/firebase-functions",
  "version": "0.0.0",
  "private": true,
  "description": "Firebase functions for my personal website",
  "author": "Teimur Gasanov",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup",
    "deploy": "yarn build && firebase deploy --only functions",
    "dev": "yarn build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "@upstash/qstash": "^2.7.23",
    "firebase-admin": "^13.2.0",
    "firebase-functions": "^6.3.2",
    "openai": "^5.12.2",
    "typescript": "^5.7.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "firebase-tools": "^14.0.1",
    "tsup": "^8.4.0"
  },
  "engines": {
    "node": "20"
  },
  "packageManager": "yarn@4.4.0"
}
